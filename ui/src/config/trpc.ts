// import { createTRPCClient, httpBatchLink } from "@trpc/client";

// export const trpcClient = createTRPCClient({
//   links: [httpBatchLink({ url: "http://localhost:3001/trpc" })],
// });

export const trpcClient = undefined;
